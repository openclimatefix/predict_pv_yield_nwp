name: predict_pv_yield_nwp
channels:
  - conda-forge
dependencies:
  - python>=3.8
  - numcodecs
  - zarr
  - pytest
  - pandas
  - matplotlib
  - xarray
  - s3fs
  - boto3
  - cartopy
  - dask[array]
  - netcdf4
  - cfgrib
  - black
